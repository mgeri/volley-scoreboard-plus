import { KeyboardShortcuts } from './keyboardShortcuts.model';

describe('KeyboardShortcuts', () => {
  it('should create an instance', () => {
    expect(new KeyboardShortcuts()).toBeTruthy();
  });
});
